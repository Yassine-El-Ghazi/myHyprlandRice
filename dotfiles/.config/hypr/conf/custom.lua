-- Add portable personal configuration here. This file is tracked, so changes
-- follow you to another computer after you commit and push them.

-- Example shortcut:
-- hl.bind("SUPER + SHIFT + N", hl.dsp.exec_cmd("obsidian"), {
--     description = "Open notes",
-- })

-- To replace an existing shortcut, unbind the exact key string first:
-- hl.unbind("SUPER + B")

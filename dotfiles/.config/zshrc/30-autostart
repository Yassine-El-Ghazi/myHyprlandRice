# -----------------------------------------------------
# AUTOSTART
# -----------------------------------------------------

# The 'ff' command (Fuzzy Finder)
finder() {
    # Run the script and capture the result
    local result
    # Open fd 3 before command substitution redirects stdout. A selected file
    # can then use the caller's terminal while stdout carries directory data.
    {
        result=$("$HOME/.config/myhypr/bin/myhypr-finder.sh")
    } 3>&1

    # Check if the script told us to CD
    if [[ "$result" == TYPE_DIR:* ]]; then
        local target_dir="${result#TYPE_DIR:}"
        cd "$target_dir" && ls
    fi
}

# -----------------------------------------------------
# Fastfetch
# -----------------------------------------------------
if [[ -o interactive && -t 1 ]] && (( $+commands[fastfetch] )); then
    fastfetch
fi

. "$HOME/.cargo/env"

# Keep user-installed programs available without letting them shadow trusted
# system executables inherited by the graphical session.
export PATH="/usr/local/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:$PATH"

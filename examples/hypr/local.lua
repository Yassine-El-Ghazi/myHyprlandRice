-- Copy to ~/.config/hypr/local.lua for host-specific output or device rules.
-- This file is loaded after the portable configuration and is ignored by Git.

-- Example: pin a monitor by its stable EDID description instead of a port name.
-- hl.monitor({
--     output = "desc:Vendor Product Serial",
--     mode = "preferred",
--     position = "auto-right",
--     scale = "1",
-- })
